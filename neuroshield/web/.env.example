# Base URL of the NeuroShield FastAPI backend.
# The WebSocket URL is derived from this automatically (http -> ws), see lib/api.ts.
# Copy to .env.local to override.
NEXT_PUBLIC_NEUROSHIELD_API_URL=http://127.0.0.1:8000
